rpi:
  username: null
  hostname: null

device: adafruit

# pattern: random
pattern: rect
# pattern: circ
rect_shape: [20, 10]  # if pattern: rect
radius: 20            # if pattern: circ

z: 4   # mask to sensor distance
